GET     /                                   cielago.controllers.Main.index()

POST    /track                              cielago.controllers.Main.handleForm()
GET     /track                              cielago.controllers.Main.handleForm()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                       controllers.Assets.at(path="/public", file)
